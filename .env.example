# Production canonical origin. Set before building.
NEXT_PUBLIC_SITE_URL=https://jingcaithread.com
# Set false for staging/private previews; true for the public production domain.
SEO_INDEXABLE=true
# Optional Search Console HTML verification token (not a Google API key).
GOOGLE_SITE_VERIFICATION=
